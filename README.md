### To start

```
git clone git@github.com:chrisdel101/f1-web.git
cd f1-web && npm install
npm start

Navitgate to localhost:3000 - make sure API server is running. That repo for start instructions.

```
