extends layout

include mixins/_select
include mixins/_card
block content
  h1= title
  //- +select({
  //-   method:method,
  //-   action: action,
  //-   enctype: enctype,
  //-   selectName: selectName,
  //-   enums: driverEnums, 
  //-   buttonType: buttonType,
  //-   buttonValue: buttonValue,
  //-   buttonField: buttonField 
  //- })
  - console.log('Enum', teamEnums)
  +select({
    method:method,
    action: action,
    enctype: enctype,
    selectName: selectName,
    enums: teamEnums, 
    buttonType: buttonType,
    buttonValue: buttonValue,
    buttonField: buttonField 
  })
  .driver-card
    +card(driverData)